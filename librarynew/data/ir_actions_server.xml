<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="stock_rule_set_push_domain_server_action" model="ir.actions.server">
        <field name="name">Execute Code</field>
        <field name="model_id" ref="stock.model_stock_rule"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
categories = record.x_pos_product_category_ids.ids if record.x_pos_product_category_ids else []
if categories:
    record['push_domain'] = [('product_id.pos_categ_ids', 'in', categories)]
else:
    record['push_domain'] = []
]]>        </field>
    </record>
    <record id="product_auto_assign_routes_action" model="ir.actions.server">
        <field name="name">Auto Assign Routes to Product</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for record in records:
    routes = env['stock.route'].search([('x_automatic_activation_on_product', '=', True)])
    if routes:
        record.write({'route_ids': [(4, route.id) for route in routes]})
]]>
        </field>
    </record>
</odoo>