<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <record id="action_check_partner_barcode_conflict" model="ir.actions.server">
        <field name="name">Check Partner Barcode Conflict</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for partner in records:
    if not partner.barcode:
        continue
    conflict = env['event.registration'].search_count([('barcode', '=', partner.barcode)], limit=1)
    if conflict:
        raise UserError(f'This barcode is already used by an event registration. Please use a different barcode.')
]]></field>
    </record>
    <record id="action_generate_event_registration_barcode" model="ir.actions.server">
        <field name="name">Generate Event Registration Barcode</field>
        <field name="model_id" ref="event.model_event_registration"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for record in records:
    barcode = record.barcode
    while env['res.partner'].search_count([('barcode', '=', barcode)], limit=1):
        barcode = env['event.registration']._get_random_barcode()
    record.write({'barcode': barcode})
]]></field>
    </record>
</odoo>
